<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/create_item_dialog"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:background="@color/colorBackgroundBasic"
    android:orientation="vertical">

    <EditText
        android:id="@+id/item_edit_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:imeOptions="actionDone"
        android:inputType="text"
        android:textColor="@color/colorTextBasic" />

    <RadioGroup
        android:id="@+id/create_item_radio_group"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:checkedButton="@+id/create_item_folder_radio">

        <RadioButton
            android:id="@+id/create_item_folder_radio"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:checked="true"
            android:text="create Folder"
            android:textColor="@color/colorTextBasic"
            android:textColorLink="@color/colorTabLayoutIndicator" />

        <RadioButton
            android:id="@+id/create_item_file_radio"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="create File"
            android:textColor="@color/colorTextBasic" />

    </RadioGroup>
</LinearLayout>